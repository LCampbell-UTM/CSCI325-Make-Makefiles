This repository serves as an assignment submission, intended to demonstrate knowledge of Make utilization and Makefile creation. 

The program itself outputs six tests, two for each of the included classes. One test is uninitialized, and the other is initialized.

Comments reside in the Makefile itself, detailing each step of the creation of the Makefile. 
